version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
    volumes:
      - ./tdlib-db:/app/tdlib-db
    env_file:
      - .env